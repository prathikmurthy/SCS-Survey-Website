import {react, Component} from 'react';

export default class PlanningIdea {
    
    constructor(id, notes, image, url) {
        this.id = id;
        this.notes = notes;
        this.image = image;
        this.url = url;
    }

}