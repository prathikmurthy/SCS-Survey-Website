import {react, Component} from 'react';

export default class PlanningIdea {
    
    constructor(id, image, notes, url) {
        this.id = id;
        this.image = image;
        this.notes = notes;
        this.url = url;
    }

}